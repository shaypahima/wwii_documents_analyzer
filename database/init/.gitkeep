# This file ensures the database/init directory is preserved in version control.
# Add your database initialization scripts (.sql files) here if needed.
# These scripts will be automatically executed when the PostgreSQL container starts for the first time. 